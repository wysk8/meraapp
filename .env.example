# Copia este archivo a ".env" y llena los valores desde tu proyecto de Supabase
# (Project Settings → API). Mientras no exista ".env", la app funciona en modo
# local con localStorage, sin login y sin base de datos real.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
